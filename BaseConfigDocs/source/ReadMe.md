To run the Asentry Base Config Setup for Linux servers follow steps provided. 
Run the bash file located in the BaseConfigDocs directory on the server 
(should be in the same folder as this document). 

./run_base_config.sh

From there follow the menu for what items to be setup and installed. 
Any option can be run independently from one another. To fully set up the server 
run every option. 
