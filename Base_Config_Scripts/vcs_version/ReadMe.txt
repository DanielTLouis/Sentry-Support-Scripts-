To run the v8 installer script:

1. Upload the desired version of server_install.jar into this directory 

2. Add the license to /usr/vcs/cfg/

3. Run the installer through the shell script run_base_config.sh 